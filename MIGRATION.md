# セットアップ手順

## 現在の状況

対象プロジェクト（`jmsvvjkvxojunofebsiy`）の `public` スキーマは**空**です。
移行すべき既存データが無いので、**スキーマを1回流すだけ**で使い始められます。

旧データから移行する場合の手順は末尾の「付録」にあります。

---

## 1. 接続設定（完了済み）

`.streamlit/secrets.toml` に `[supabase]` の `url` と `publishable_key` を記入します。
設定済みで、キーの種類も正しい（`sb_publishable_...`）ことを確認しました。

## 2. スキーマを作る

```bash
.venv/Scripts/python.exe -m mahjong.migrator
```

`schema.sql` が生成されます（`003a_groups_schema.sql` + `003c_rls.sql` + `003d_views_rpc.sql`）。
中身を全部コピーして、**Supabase ダッシュボード → SQL Editor** に貼り付けて Run。

これ1回で、テーブル・インデックス・外部キー・RLS・ビュー・RPC関数がすべて揃います。
冪等なので、失敗しても直して何度でも流し直せます。

> `migrations/oneshot/` の中身は**実行しないでください**。
> 旧データからの移行専用で、空のDBには不要です。

## 3. 動作確認

```bash
.venv/Scripts/python.exe -m streamlit run app.py
```

1. **アカウントを作る** — 「新規登録」タブから。
   Supabase の設定でメール確認が有効なら、確認メールのリンクを開いてからログインします。
2. **グループを作る** — ログインすると「はじめに」画面が出ます。
   グループ名（例:「金曜麻雀会」）と自分の表示名を入れて作成。
3. **大会 → 開催日 → 卓 → スコア** の順に作って、1半荘入力してみる。
4. **招待** — メンバー画面で招待コードを発行し、他の人に渡す。
   相手はアカウントを作ってコードを入れるだけで、そのグループの全履歴が見えます。

## 4. 複数人で使えることの確認（重要）

これが今回の作り直しの本丸です。**1つのブラウザでは検出できません。**

1. 通常のウィンドウでアカウントAでログイン
2. **シークレットウィンドウ**（別ブラウザでも可）でアカウントBを新規登録し、招待コードで参加
3. 両方を開いたまま、交互に操作して次を確認する
   - どちらかの表示が相手のアカウントに切り替わらないこと
   - 片方がログアウトしても、もう片方が落ちないこと
   - 同じグループの履歴が両方から見えること
4. **グループに参加していない3人目**でログインすると、何も見えないこと（RLSの確認）

## 5. RLSが実際に効いていることの確認（任意だが推奨）

SQL Editor は `postgres` として動くため**RLSを素通りします**。
「ダッシュボードで見えたから大丈夫」は検証になりません。
`migrations/oneshot/04_verify.sql` の5番のブロック（コメントアウトしてあります）を
実際のユーザーIDに置き換えて実行してください。

特にこの1行が **42501 で失敗する**ことを確認します。

```sql
UPDATE public.players SET role = 'owner' WHERE user_id = auth.uid();
```

RLSポリシーだけでは「自分のグループの自分の行を更新する」操作を止められません
（行は前も後も自分のグループのもので、条件を満たしてしまうため）。
列単位の GRANT だけがこれを防げます。ここが効いていないと、
メンバーの誰でも自分をオーナーに昇格させられます。

---

## うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| `Could not find the table 'public.xxx'` | PostgREST のスキーマキャッシュが古い。`NOTIFY pgrst, 'reload schema';` を単体で実行するか、少し待って再読み込み |
| `permission denied for table ...` | `003c_rls.sql` の GRANT 部分が流れていない。003c を流し直す |
| `permission denied for column role` | **正常**。メンバーが自分を昇格させようとしたときに出るべきエラー |
| `infinite recursion detected in policy for relation "players"` | どこかで `FORCE ROW LEVEL SECURITY` が付いている。外す |
| ログインしても何も見えない | そのアカウントがどのグループにも属していない。招待コードで参加するか、グループを作る |
| 通信エラーの表示が英語のまま | `mahjong/errors.py` を通っていない箇所。報告してください |

---

## 付録: 旧データから移行する場合

バックアップから旧スキーマ（`titles` / `players` / `games` / …）を復元して移行する場合は、
`migrations/oneshot/` を次の順に使います。詳細は `migrations/oneshot/README.md`。

1. `00_inventory.sql` — 現状の棚卸し（読み取り専用）
2. `00_backup.sql` — `bak_003` スキーマへ複製
3. `01_preflight.sql` — **統合される同名プレイヤーの確認（読み取り専用）**
4. `003a_groups_schema.sql`
5. `02_data_migration.sql` — **一度きり**。グループ作成・同名統合・開催日生成
6. `003c_rls.sql` → `003d_views_rpc.sql`
7. `04_verify.sql` — 検算

`01_preflight.sql` の中止条件（P4/P5/P6）が1行でも返したら、5番に進まないこと。
同じ半荘に同名の別プレイヤーがいる等、統合すると一意制約に衝突します。
